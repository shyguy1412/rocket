import style from './Settings.module.css';
import { h } from 'preact';
import { memo } from 'preact/compat';

namespace Settings {
    export type Props = {};
}

export const Settings = memo(({}: Settings.Props) => {
    return <div></div>;
});
